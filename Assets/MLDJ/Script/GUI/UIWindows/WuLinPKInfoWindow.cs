using UnityEngine;
using System.Collections;

public class WuLinPKInfoWindow : MonoBehaviour {
/*
    public TabController m_WuLinTabController;
    public WuLinRoundPKInfoWindow[] m_RoundWindow;

    void OnEnable()
    {
        WuLinData.delegateShowPkInfo += ShowPKInfoList;
    }

    void OnDisable()
    {
        WuLinData.delegateShowPkInfo -= ShowPKInfoList;
    }

    public void OnReturnButtonClick()
    {
        if (m_WuLinTabController != null)
        {
            m_WuLinTabController.ChangeTab("Tab1");
            WuLinData.delegateShowPkInfo -= ShowPKInfoList;
        }
    }
   
    public void ShowPKInfoList()
    {
        for (int i = 0; i < m_RoundWindow.Length; i++)
        {
            m_RoundWindow[i].gameObject.SetActive(false);
        }
        if (WuLinData.Rounder <= m_RoundWindow.Length && WuLinData.Rounder >= 1)
        {
            m_RoundWindow[WuLinData.Rounder - 1].gameObject.SetActive(true);
            m_RoundWindow[WuLinData.Rounder - 1].UpdatePkInfo();
        }
        else
        {
            if (m_RoundWindow.Length >=1)
            {
                m_RoundWindow[m_RoundWindow.Length - 1].gameObject.SetActive(true);        
            }
        }
    }
 */ 
}
